spring:
  ai:
    vertex:
      ai:
        gemini:
          project-id: ${GCP_PROJECT_ID:your-gcp-project-here}
          location: ${GCP_LOCATION:europe-west1}
          chat:
            options:
              model: gemini-2.0-flash-exp
